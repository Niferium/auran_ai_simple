# auran_ai_simple
Using a windows machine, create an ai assistant that can connect to other LLMS to be your front man


# Requirements:
- pip install requests torch transformers
- pip install colorama   # optional, improves colour on older Windows terminals